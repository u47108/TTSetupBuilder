import { ittfEquipmentApproval } from '../sources/registry.js';

/**
 * ITTF equipment approval — official lists via ittf-admin-api (batch only).
 * Prefer `pnpm ittf -- snapshot|annotate` over SPA HTML scraping.
 */
export { ittfEquipmentApproval as source };
