import { ittfEquipmentApproval } from '../sources/registry.js';

/**
 * ITTF equipment approval — SPA hash route.
 * API discovery required before any live fetch (see docs/DATA_SOURCES.md).
 */
export { ittfEquipmentApproval as source };
