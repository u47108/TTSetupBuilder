# apps/web

Next.js application for TTSetupBuilder.

**Status:** Not bootstrapped yet.

Planned create command (do not run until architecture docs exist):

```bash
npx create-next-app@latest apps/web
```

Options: TypeScript, ESLint, Tailwind, `src/`, App Router, Turbopack, import alias `@/*`.
